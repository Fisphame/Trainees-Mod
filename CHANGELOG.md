# 更新日志（模组自身）

> 说明：根目录的 `changelog.txt` 是 **Forge 自带的**更新日志模板，与本模组无关，请勿混淆；模组自身的改动记录在本文件。
> 版本号由作者决定（当前 `gradle.properties` 仍为 `1.4.0a`）。下面「未发布」一节 = 当前工作树相对 `origin/master`（`4459140`）的全部改动。

## 未发布（准备发布：阶段完整版）

### 玩法新增

**含氯消毒液化学（§19.18 / Step ②）**
- 新增离子 `trainees:clo_minus`（次氯酸根，有效氯的载体）与 `trainees:clo3_minus`（氯酸根）
- 规则 `chlorine_disproportionation`：`Cl₂ + 2OH⁻ → ClO⁻ + Cl⁻ + H₂O`（低温路径，几乎无势垒）
- 规则 `hypochlorite_disproportionation`：`3ClO⁻ → ClO₃⁻ + 2Cl⁻`（高温路径，活化能高）
- **机理**：高温出氯酸盐**不是**"氯直接与热碱反应"，而是次氯酸盐受热歧化——两条规则是**串联**的，于是"低温 → 次氯酸盐 / 高温 → 氯酸盐"由温度依赖的反应速率自然涌现，无需手写条件分支
- **修复缺口**：`OH⁻` 此前在反应图中只被消耗、从不产生 → 新增 `naoh_dissociation`（NaOH 遇水给出 `OH⁻`），"碱"这条线终于可以从零开始

**相变系统（§19.17.1 / Step ③）**
- 水的三相（冰 `h2o_solid` / 水 / 蒸汽）+ 食盐固液，共 **8 条有向边**
- 熔点/沸点由**热力学**决定（物理平衡常数在转变温度处穿过 1），潜热经引擎热反馈自动生效 → 熔化时温度会"卡"在熔点，即**相变平台**
- 引擎新增：规则**温度窗口上界**（`maxTemperature`）、相变规则的 `quiet` 标记（不写失败诊断、不刷分析仪，也不进 JEI 的化学反应百科）
- 数值自洽由单测保护：**由生成量推出的熔点/沸点与文献值偏差 ≤5%**

**热源分级与控温（§19.24 / Step ③）**
- 新增**设定点型热源**接口：既能加热、也能从容器**吸热**（与"燃料型热源只能单向烤热"相对）
- **创造热源**：目标温度 1~3000 K（**含 0 ℃ 以下**）；右键升档 / 潜行右键降档，`/chemtester heat <K>` 可指定任意值
- **恒温浴**：四种介质决定可达区间——冰盐浴 ≤0 ℃、水浴 ≤100 ℃、油浴 ≤250 ℃、沙浴 ≤600 ℃；换介质时设定点自动夹进新区间

**容器材料分级（§6.3 / Step ③）**
- 四档：玻璃烧杯 **773 K**、金属坩埚 1673 K、石英坩埚 1873 K、陶瓷坩埚 **2273 K**（热容与换热系数各不相同）
- 炼钢（1600 K+）与熔盐（1074 K）**必须换坩埚**——科技树的第一道**材料硬墙**
- 既有玻璃烧杯行为**完全不变**（基准倍率 1.0）

**打包机与日化品（§19.18 / Step ④）**
- 新方块**打包机**：读取其**正上方容器**（烧杯/坩埚/电解槽），按规格质检后灌装
- 含氯消毒液规格：**有效氯** 合格 5–8%、优质 6–7.5%；门禁 游离氯**禁用** / 游离酸 ≤0.05% / 游离碱 ≤1% / 氯酸盐 ≤1%
- **有效氯按国标口径折算**（1 mol ClO⁻ ≡ 1 mol Cl₂）→ 判定直接用国标百分数，图鉴里会点明这个概念
- **打包机自动化（2026-09-25）**：新增方块实体——**每 2 秒自驱一次**（永远 auto 模式），产物进**输出槽**（`IItemHandler`，ME/漏斗可抽，**槽满停产**），并加 **FE 门槛**（每瓶默认 **20 kJ = 20000 FE**，1 FE = 1 J；**没电或槽满都不扣料**）。交互随之改为：**空手右键取产物**、**潜行空手右键人工强制灌装**（唯一能产危险次品的入口）、手持物品不拦截。配置组 `packer`（`energyPerBottle` / `energyCapacity` / `checkInterval`）
- 次品**按失败类别**共用物品：游离氯超标（放毒）/ 碱超标（腐蚀）/ 酸超标（放气）/ 氯酸盐超标（氧化性、中毒）；量不足（灌不满一瓶）任何模式都不产出
- **次品只为"危险批次"存在**：浓度不符（有效氯过低/过高）时**任何模式都不灌装**——它既不危险也没有教学价值，产出来只是垃圾，还会把"次品"的语义从"危险品"污染成"不达标商品"
- 扣料遵循"理想搅拌"不变量：按比例取走一瓶，**不改变容器内成分比例**
- **次品使用时反噬**（行为钩子）：游离氯 → 中毒（放毒）；碱超标 → 化学烧伤；酸超标 → 自毁放气（伤害 + 直接消耗）；氯酸盐 → 中毒。创造模式不消耗物品

### 修复
- **旧存档无法打开**（区块加载自死锁）：`GasGridManager` 在 `ChunkEvent.Load` 中同步获取方块状态
- **专用服务器启动崩溃**：数据包类在字节码里引用了客户端类（`Screen` 等）
- **成就 `trainees:entity/kun_anti_boss` 加载失败**：`parent` 指向错误
- **烧杯与电解槽此前没有战利品表** → 生存模式挖掉会直接消失（已补，含打包机/坩埚/恒温浴/创造热源）
- 烧杯耐温上限原先由 `static final` 只读一次配置 → 改为按材料取值（顺带修掉"改配置不生效"的隐患）

### 工程与文档
- 新增规格判定纯逻辑层 `chemistry/spec/`（键空间契约、verdict 优先级、失败关闭、`othersFraction` 语义）
- 单元测试 **17 条 → 77 条**（ReactionMath 17 / SpecMatcher 27 / IonDisplay 2 / PhaseChangeTable 8 / ContainerMaterial 5 / PackerService 18）
- 中英文本地化 **548 : 548** 完全对齐（每次构建都会对拍）
- 蓝本新增 §19.17.1 实现记录、§19.18.1–19.18.4、§19.24，并记录了热源/轮询时机的硬约束计算

### 已知问题（发版前需处理）
1. **缺失贴图 51 处**（金属矿/锭、化学粉末、离子交换膜等），且本次新增方块/物品均使用**原版占位贴图**（打包机、三种坩埚、恒温浴、创造热源、6 个产物）——清单见 `RESOURCES_MISSING.md`
2. 电解槽方块模型仍是**铁块占位**
3. 打包机**暂无 GUI**（目标产品选择、强制灌装按钮、复用诊断面板）
4. 产品的 behavior key 目前**只记录不驱动**（按 §19.18 的决定，用途机制后置）；合格/优质消毒液暂无使用效果
5. AI 助教仍为 S0 原始形态（`/ask`）；市场与科技树节点（⑤⑥）未开始
6. 版本号仍为 `1.4.0a`，待作者决定正式版号

---

*维护：每次发版时把「未发布」整理到对应版本号之下。*
