# 进游戏测试样例（②③④ + 打包机）

> 适用版本：本次工作树的构建（`gradlew build` 出的 jar 或 `gradlew runClient`）。
> 需要**权限等级 2** 才能用 `/chemtester`（单人：开作弊/对局域网开放并允许作弊；服务器：op）。
> 命令都要**准星对准目标方块**再执行。所有"预期"里，标注**理论值**的是按热力学/化学计量算出来的，允许有偏差；标注**硬性**的必须成立。

## 0. 一分钟快速版（三条成败线）

| # | 操作 | 必须看到 |
|---|---|---|
| Q1 | 热源设 **293 K**，次氯酸盐溶液放置一会儿 | **不出现** `clo3_minus` |
| Q2 | 热源改 **353 K** | **出现** `clo3_minus` |
| Q3 | 玻璃烧杯 + 热源设 **900 K** | 温度被钳在 **773 K** |

三条都过 → ②③ 的核心机制成立。下面是要看细节的完整版。

---

## 1. 碱线（② 顺带修的缺口：OH⁻ 此前只消耗不产生）

```
放一个烧杯 → /chemtester fluid fill 1000        （灌水，= 64 mol H₂O）
/chemtester add trainees:naoh 4                  （4 mol 固体烧碱）
等 1~2 秒 → /chemtester info
```
- **硬性**：出现 `oh_minus`（NaOH 遇水电离成 Na⁺ + OH⁻）
- 理论值：`oh_minus` ≈ 4 mol、`na_1` ≈ 4 mol，`naoh` 基本耗尽
- 若没反应：确认容器里有水（`h2o` 不为 0）——该规则要求水做前置条件

## 2. 冷路径（氯气被碱吸收 → 次氯酸盐）

```
/chemtester add trainees:cl2 2
等 2~5 秒 → /chemtester info
```
- **硬性**：出现 `clo_minus`、`cl_minus`
- 理论值（配 4 mol OH⁻ + 2 mol Cl₂）：`clo_minus` ≈ 2、`cl_minus` ≈ 2，`oh_minus` 基本耗尽
- ⚠ Cl₂ 是**气体**、烧杯敞口会向网格逸散：加完立刻看，或把 Cl₂ 一次加成 1.1 倍余量

## 3. 温度门控（② 的成败线：冷/热两条路径）

**先造一批次氯酸盐**（用上面 T1+T2 的配方），然后：

| 步骤 | 热源设定 | 预期 |
|---|---|---|
| 3a | **293 K** 放 20~30 秒 | **硬性**：`clo3_minus` 不出现（或 < 0.01 mol） |
| 3b | 改 **353 K**（或恒温浴水浴 353 K）放 20~30 秒 | **硬性**：`clo3_minus` 开始累积，`clo_minus` 下降 |
| 3c | 再降回 293 K | 歧化显著变慢（这是同一批料的对照） |

- 原理：`3ClO⁻ → ClO₃⁻ + 2Cl⁻` 的 Ea 高（85 kJ/mol），室温几乎冻结、60~80 ℃ 才显著
- 理论温度：热源 353 K → 烧杯平衡在 **≈352 K**（正好落在 60~80 ℃ 窗口内）

## 4. 相变平台（③：熔点/沸点由热力学决定，潜热自动生效）

```
玻璃烧杯 → /chemtester add trainees:h2o_solid 10     （放 10 mol 冰）
/chemtester heat 300                                   （热源 300 K）
连看几次 /chemtester info
```
- **硬性**：出现 `h2o`（冰熔化）
- **看点**：温度上升会在 **273 K 附近明显变慢/停一下**（熔化吸热 = 潜热），这就是"相变平台"
- 继续测：
  - 热源改 **260 K** → 冻回 `h2o_solid`
  - 热源改 **380 K** → 出现 `h2o_gas`（沸点 ≈369~373 K，理论 373.15）
  - `/chemtester gas info` 可以看到敞口逸散出去的蒸汽（Phase 9 网格）

## 5. 材料硬墙（③-3）

| 容器 | 热源设定 | **硬性预期** |
|---|---|---|
| 玻璃烧杯 | 900 K | 温度停在 **773 K** |
| 石英/陶瓷坩埚 | 900 K | 能继续升到 773 K 以上（趋向 ≈880 K） |

## 6. 熔盐电解（③：先熔化才能电解，且电解吸热会把它冻回去）

```
陶瓷坩埚 → /chemtester add trainees:nacl 10
/chemtester heat 1100          （需要 ≥1074 K，玻璃烧杯做不到）
等 → /chemtester info          → 出现 nacl_molten
/chemtester power true         （若默认配置已"无限电"，可跳过）
继续等 → info                  → 出现 na 与 cl2
```
- **硬性**：`nacl_molten` 出现后才有 `na` / `cl2`
- **看点**：电解吸热会把温度拉回熔点以下 → 盐冻回去 → 反应暂停；热源持续供热才断断续续地产出（设计中的正反馈，不是 bug）

## 7. 打包机与日化品（④）

**摆法**：打包机放地上，**容器（烧杯/坩埚）放在打包机正上方**。

**现在的交互（自动化版，2026-09-25 起）**
- 打包机**自己干活**：**通电 + 溶液合格 + 输出槽有空位** → 每 2 秒自动打一瓶（**永远 auto 模式**，不合格不产不扣）
- **空手右键 = 取走输出槽产物**；**潜行空手右键 = 人工强制灌装**（唯一能产危险次品的入口）；手持物品右键不拦截
- **FE**：每瓶 **20 kJ（= 20000 FE，1 FE = 1 J）**，任意一面都能充；**电不够 → 不打包、不扣料**
- **输出槽**是 `IItemHandler`（外部只能抽）：AE2 输出总线 / 漏斗 / 管道可直接取货；**槽满 → 停产**（同样不扣料）
- 配置组 `packer`：`energyPerBottle`（默认 20000）/ `energyCapacity`（400000）/ `checkInterval`（20 tick）

| 目标 | 投料（1000 mB 水 = 64 mol 为基准） | 预期 |
|---|---|---|
| **合格品** | `add trainees:naoh 2` → `add trainees:cl2 1` | 自动产出一瓶**含氯消毒液**（有效氯 ≈5.4%）→ 空手右键取出 |
| **优质品** | `add trainees:naoh 2.4` → `add trainees:cl2 1.2` | 自动产出**优质含氯消毒液**（有效氯 ≈6.4%） |
| **游离氯次品** | `add trainees:naoh 0.5` → `add trainees:cl2 1`（碱不足，氯过量） | 自动**拒收**（不产不扣）；**潜行右键**强制灌装 → `defective_chlorine` |
| **浓度不符（不产次品）** | `add trainees:naoh 0.6` → `add trainees:cl2 0.3`（太稀） | **硬性**：自动与强制**都不产出**，提示"成分不符且无危险成分" |
| **氯酸盐次品** | 先在 353 K 放一会儿把一部分 ClO⁻ 歧化成 ClO₃⁻（>1%） | 自动拒收；强制灌装 → `defective_chlorate` |
| **量不足** | `/chemtester clear` → `add trainees:h2o 0.5` | 提示"溶液不足一瓶"，**不产出也不扣料** |
| **没电** | 断开供电再配好溶液 | 一直不产瓶；`info` 确认总量没变（没白扣料） |
| **输出槽满** | 连打多瓶都不取出 | 停止产出（潜行右键的提示会说明）；取走产物后自动恢复 |

- **硬性**：任何失败（不合格 / 量不足 / **没电** / **槽满**）都**不扣料**（用 `info` 看总量是否没变）
- **硬性**：`defective_chlorine` 拿在手里**右键自己** → 中毒（放毒）；`defective_alkali` → 掉血；`defective_acid` → 掉血且物品消失；`defective_chlorate` → 中毒
- **自动化验证**：输出槽接 AE2 输出总线（或漏斗）→ 产物被自动抽走且机器持续出瓶；任意一面接 FE 电源即可（`IEnergyStorage`，只进不出）
- **硬性**：`defective_chlorine` 拿在手里**右键自己** → 中毒（放毒）；`defective_alkali` → 掉血；`defective_acid` → 掉血且物品消失；`defective_chlorate` → 中毒。（**没有**"浓度不符"次品：那种批次根本不会灌装）
- ⚠ **容量不够时按同比例缩放**：规格判定是**质量分数**口径，所有物料（水/碱/氯）成比例缩小不影响结论。例：水只灌得进 500 mB，就把碱和氯都减半
- ⚠ 投料比例是"理论收率"下的计算值；实际有气体逸散，结果可能差一档——这正好用分析仪迭代（§19.18 的"规格有牙齿"）

## 8. 不刷屏与图鉴（收尾项）

- `/chemtester failures`：相变规则标了 `quiet`，**不应**被 `phase_*` 塞满
- JEI 搜"含氯消毒液" → 信息页应显示**有效氯 = ClO⁻ 折算 Cl₂ 当量**、合格 5–8% / 优质 6–7.5%、四项门禁
- JEI 的"化学反应"列表里**不应**出现 `phase_h2o_fusion` 等相变条目（相变是物理过程）
- 分析仪（右键容器）→ 诊断面板：温度、内容物、最近失败与建议

---

## 出问题请给我这些

1. 当时的投料与热源设定（例：`fluid fill 1000` / `naoh 2` / `cl2 1` / 热源 353 K）
2. `/chemtester info` 的完整输出
3. `/chemtester failures` 或 `/chemtester last`（如果涉及"该反应没发生"）
4. `logs/latest.log` 里的相关片段（若有异常/报错）

有这四项，我基本能直接定位到具体规则或数值，不用猜。
